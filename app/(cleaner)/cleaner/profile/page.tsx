import { CleanerProfileForm } from "@/components/cleaner/cleaner-profile-form";import { getCleanerContext } from "@/lib/cleaner/server";import { createServerClient } from "@/lib/supabase/server";import type { Profile } from "@/types/auth";
export default async function CleanerProfilePage(){const s=createServerClient();const {data:{user}}=await s.auth.getUser();const c=await getCleanerContext(s,user!.id);return <div><h1 className="text-3xl font-semibold">Profile</h1><p className="mt-2 mb-7 text-muted-foreground">Manage your services, areas, documents, and payout preferences.</p><CleanerProfileForm areas={c.areas} availability={c.availability} cleaner={c.cleanerProfile} profile={c.profile as Profile} services={c.services}/></div>}
