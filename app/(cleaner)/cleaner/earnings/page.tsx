import { getCleanerContext, getCleanerJobs } from "@/lib/cleaner/server";
import { formatMoney, formatServiceName } from "@/lib/customer/services";
import { createServerClient } from "@/lib/supabase/server";
import { EarningsControls } from "@/components/cleaner/earnings-controls";
import type { Payout } from "@/types/cleaner";
export default async function CleanerEarningsPage(){const s=createServerClient();const {data:{user}}=await s.auth.getUser();const [ctx,jobs,{data:payouts}]=await Promise.all([getCleanerContext(s,user!.id),getCleanerJobs(user!.id),s.from("payouts").select("*").eq("cleaner_id",user!.id).order("created_at",{ascending:false})]);const done=jobs.filter(j=>j.status==="completed"),sum=(days:number)=>done.filter(j=>Date.now()-new Date(j.scheduled_date).getTime()<days*864e5).reduce((n,j)=>n+(j.amount_cleaner??0),0),all=done.reduce((n,j)=>n+(j.amount_cleaner??0),0);
return <div><h1 className="text-3xl font-semibold">Earnings</h1><div className="mt-7 grid gap-4 sm:grid-cols-3"><Card l="This week" v={formatMoney(sum(7))}/><Card l="This month" v={formatMoney(sum(31))}/><Card l="All time" v={formatMoney(all)}/></div><EarningsControls preference={ctx.cleanerProfile.payout_preference}/><div className="mt-7 overflow-x-auto rounded-xl border bg-background"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-3">Date</th><th>Service</th><th>Gross</th><th>Commission</th><th>Net</th></tr></thead><tbody>{done.map(j=><tr className="border-b last:border-0" key={j.id}><td className="p-3">{j.scheduled_date}</td><td>{formatServiceName(j.service_type)}</td><td>{formatMoney(j.amount_total)}</td><td>{formatMoney((j.amount_total??0)-(j.amount_cleaner??0))}</td><td className="font-semibold">{formatMoney(j.amount_cleaner)}</td></tr>)}</tbody></table></div><h2 className="mt-8 text-xl font-semibold">Payout history</h2><div className="mt-3 space-y-2">{((payouts??[]) as Payout[]).map(p=><div className="flex justify-between rounded-lg border bg-background p-4" key={p.id}><span>{p.period_start} · <span className="capitalize">{p.status}</span></span><b>{formatMoney(p.net_amount)}</b></div>)}</div></div>}
function Card({l,v}:{l:string;v:string}){return <div className="rounded-xl border bg-background p-5"><p className="text-sm text-muted-foreground">{l}</p><p className="mt-1 text-2xl font-bold">{v}</p></div>}
