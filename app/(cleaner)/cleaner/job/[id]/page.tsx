import { notFound } from "next/navigation";import { JobDetail } from "@/components/cleaner/job-detail";import { createAdminClient } from "@/lib/supabase/admin";import { createServerClient } from "@/lib/supabase/server";import type { CleanerJob } from "@/types/cleaner";
export default async function CleanerJobPage({params}:{params:{id:string}}){const s=createServerClient();const {data:{user}}=await s.auth.getUser();const {data}=await createAdminClient().from("bookings").select("*,address:addresses(*),customer:profiles!bookings_customer_id_fkey(full_name,phone)").eq("id",params.id).eq("cleaner_id",user!.id).single();if(!data)notFound();return <JobDetail initialJob={data as CleanerJob}/>}
