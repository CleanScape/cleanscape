"use client";
import { useRouter } from "next/navigation";import { useState } from "react";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { formatMoney } from "@/lib/customer/services";
interface Promo{id:string;code:string;discount_type:"percentage"|"fixed";discount_value:number;max_uses:number|null;uses_count:number;valid_from:string|null;valid_until:string|null;is_active:boolean}
export function PromosManager({promos}:{promos:Promo[]}){const router=useRouter(),[form,setForm]=useState({code:"",discount_type:"percentage",discount_value:"",max_uses:"",valid_from:"",valid_until:""});async function save(payload:Record<string,unknown>){const r=await fetch("/api/admin/promos",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});if(r.ok)router.refresh()}return <div><section className="rounded-xl border bg-card p-5"><h2 className="font-semibold">Create promo code</h2><div className="mt-4 grid gap-3 md:grid-cols-3"><Input placeholder="CLEAN10" value={form.code} onChange={e=>setForm({...form,code:e.target.value.toUpperCase()})}/><select className="rounded-md border px-3" value={form.discount_type} onChange={e=>setForm({...form,discount_type:e.target.value})}><option value="percentage">Percentage</option><option value="fixed">Fixed (£)</option></select><Input min={0} placeholder="Value" type="number" value={form.discount_value} onChange={e=>setForm({...form,discount_value:e.target.value})}/><Input min={1} placeholder="Max uses" type="number" value={form.max_uses} onChange={e=>setForm({...form,max_uses:e.target.value})}/><Input type="datetime-local" value={form.valid_from} onChange={e=>setForm({...form,valid_from:e.target.value})}/><Input type="datetime-local" value={form.valid_until} onChange={e=>setForm({...form,valid_until:e.target.value})}/></div><Button className="mt-4" disabled={!form.code||!form.discount_value} onClick={()=>void save({action:"create",...form,discount_type:form.discount_type,discount_value:Number(form.discount_value),max_uses:form.max_uses?Number(form.max_uses):null})}>Create promo</Button></section><div className="mt-6 overflow-x-auto rounded-xl border bg-card"><table className="w-full min-w-[800px] text-sm"><thead><tr className="bg-muted/50 text-left"><th className="p-3">Code</th><th>Discount</th><th>Usage</th><th>Validity</th><th>Status</th><th>Analytics</th></tr></thead><tbody>{promos.map(p=><tr className="border-t" key={p.id}><td className="p-3 font-mono font-bold">{p.code}</td><td>{p.discount_type==="percentage"?`${p.discount_value}%`:formatMoney(p.discount_value)}</td><td>{p.uses_count}/{p.max_uses??"∞"}</td><td>{p.valid_from?.slice(0,10)??"Now"} – {p.valid_until?.slice(0,10)??"No expiry"}</td><td><Button onClick={()=>void save({action:"toggle",id:p.id,is_active:!p.is_active})} size="sm" variant={p.is_active?"default":"outline"}>{p.is_active?"Active":"Inactive"}</Button></td><td>{p.max_uses?Math.round(p.uses_count/p.max_uses*100):p.uses_count} {p.max_uses?"%":"uses"}</td></tr>)}</tbody></table></div></div>}
